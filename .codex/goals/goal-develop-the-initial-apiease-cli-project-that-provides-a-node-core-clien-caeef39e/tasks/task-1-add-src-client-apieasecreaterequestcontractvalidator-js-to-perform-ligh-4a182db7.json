{
  "id": "task-1-add-src-client-apieasecreaterequestcontractvalidator-js-to-perform-ligh-4a182db7",
  "text": "Add src/client/ApiEaseCreateRequestContractValidator.js to perform lightweight client-side validation for create-request payloads.",
  "goalId": "goal-develop-the-initial-apiease-cli-project-that-provides-a-node-core-clien-caeef39e",
  "goal": "Develop the initial apiease-cli project that provides a Node core client capable of creating requests through the APIEase Create Request API contract, along with a thin bash wrapper that allows users and automation tools to invoke the client from the command line.\n\nGoal\n\nCreate a new open source project named apiease-cli that implements a Node based core client for interacting with the APIEase Create Request API. The Node core client will be the canonical client implementation used by command line scripts, automation tools, and AI agents. The CLI layer will act only as a thin wrapper that invokes the Node core client.\n\nThe purpose of this task is to establish the client side contract that matches the API implemented in the apiease project and to demonstrate an end to end flow where a request definition can be submitted locally and created remotely through the API.\n\nScope\n\nImplement only the minimal functionality required to support creating requests through the APIEase API. The task should establish the structure of the apiease-cli project, implement the Node core client capable of calling the Create Request API endpoint, and provide a bash script that invokes the Node client.\n\nThe CLI should support creating requests from a structured input file and sending the request configuration to the APIEase backend through the public API.\n\nRequirements\n\nCreate a new project named apiease-cli intended to be published as an open source repository. The project must include a Node core client that acts as the primary implementation layer for interacting with the APIEase API.\n\nThe Node core client must implement a function responsible for creating a request through the APIEase API. This function must accept a structured object representing the request configuration and must send that object to the Create Request API endpoint implemented in the apiease project.\n\nThe Node core client must support authentication using the APIEase API key so that CLI users and automation tools can interact with the API.\n\nThe Node core client must perform lightweight contract validation before making the API call. Validation should confirm that the provided payload is a valid structured object and matches the expected request contract shape, but it should not attempt to duplicate full backend validation logic.\n\nThe Node core client must return structured JSON responses that clearly indicate success or failure and must surface backend validation errors in a machine readable format.\n\nCreate a thin bash wrapper script that invokes the Node core client. The bash wrapper must accept arguments indicating that a request should be created and must pass the structured request configuration to the Node client. The wrapper should not implement business logic and should only act as a bridge between the command line and the Node client.\n\nThe CLI command must support reading the request configuration from a JSON file so that requests can be authored locally and submitted to the API.\n\nThe CLI must produce output that is readable for humans but must also support machine friendly output so that automation tools and AI agents can reliably interpret the result.\n\nImplementation guidance\n\nThe Node core client should be the canonical implementation layer and should encapsulate all logic related to interacting with the APIEase API. The bash script must remain minimal and should simply forward input arguments or file input to the Node client.\n\nThe Node core client must be designed so that it can be called both by the CLI wrapper and directly by other Node based tools. The architecture should keep the client logic independent from the CLI wrapper so that the same client can be reused by other automation layers.\n\nThe request creation contract used by the Node core client must match the API contract implemented in the apiease project. The CLI must not introduce its own schema independent of the API.\n\nAvoid implementing additional commands such as pull, push, variables, widgets, diff, status, or synchronization features. This task must focus only on the minimal vertical slice needed to create a request through the API.\n\nDeliverable\n\nA working apiease-cli project containing a Node core client capable of creating requests through the APIEase API and a bash wrapper command that allows users and automation tools to submit request configurations from the command line. The client must authenticate using the APIEase API key, validate the input structure, call the Create Request API endpoint, and return structured results.",
  "project": "apiease-cli",
  "status": "complete",
  "priority": "high",
  "tags": [
    "client",
    "validation",
    "contract"
  ],
  "createdBy": "kevin",
  "createdAt": "2026-03-07T03:28:39Z",
  "updatedAt": "2026-03-07T04:51:35Z",
  "context": [
    "Reference backend contract shape from ../apiease/backend/service/request/RequestCreateContractValidator.js.",
    "The client must validate against the same request contract used by the APIEase Create Request API without duplicating full backend behavior."
  ],
  "steps": [
    "Model the top-level request fields accepted by the backend create contract.",
    "Support the backend's conditional requirements for http, liquid, and system request types.",
    "Validate nested parameter and trigger collections closely enough to catch obvious contract mismatches before making the API call.",
    "Return machine-readable validation results with errorCode, message, and fieldErrors."
  ],
  "constraints": [
    "Do not implement persistence normalization or other backend-only business rules.",
    "Do not invent a CLI-specific schema that differs from the API contract.",
    "Keep field error objects aligned with the backend shape of path, code, and message."
  ],
  "acceptanceCriteria": [
    "The validator accepts structured object payloads that match the backend create contract shape.",
    "The validator rejects unsupported fields, missing required conditional fields, and invalid nested parameter or trigger shapes.",
    "Invalid payloads return a failure result using errorCode INVALID_REQUEST_CREATE_CONTRACT, message Request create payload is invalid, and machine-readable fieldErrors."
  ],
  "sequenceIndex": 1,
  "codexSettings": {
    "workingDirectory": ".",
    "branchStrategy": "current",
    "maxRuns": 1
  },
  "result": {
    "summary": "",
    "notes": [],
    "completedAt": null
  }
}
